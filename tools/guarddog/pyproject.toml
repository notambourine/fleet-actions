[project]
name = "fleet-guarddog"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["guarddog==3.2.0"]

[tool.uv]
package = false
# GuardDog's released metadata excludes the traversal fix accepted in DataDog/guarddog#858.
override-dependencies = ["tarsafe==0.0.6"]
